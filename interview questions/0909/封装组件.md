### 封装组件

1.什么样的代码该抽成公共组件?

同一段ui和交互重复至少在两个页面以上出现；内聚复杂逻辑，组件内部藏了大量外部不关心的细节；稳定的输入输出边界

2.通过什么样的方式给外部去使用这个组件？

- props：父 → 子，传数据、配置
- emit /callback：子 → 父，通知事件
- expose/ref：父主动调用子组件内部方法
- **插槽：父向子传入 DOM 模板；作用域插槽还可以把子组件的数据回传给父的模板使用**

> 补充：封装公共组件的时候，一般组合使用：优先 props 传配置；配置搞不定复杂 DOM，就上插槽。