##### 问：我看你最近的项目是那个知识产权的吧，然后里面有涉及到那个AI的智能模块的开发以及ai对话的功能，可以大概说下是咋实现的吗

##### 问：可以具体说一下你这个前端缓存是怎么做的吗

如果是问ai的这种开发，一般是问你历史对话会不会存在localstorage里面，千万不能说localstorage全部保存ai的历史对话，这是不对的。本身localstorage容量是有限的，并且api是同步的，大量的读写本身会阻塞这个主线程，要考虑到一个性能问题。适合存轻量的一些状态。可以存ai对话的conversation id，也就是当前对话的id，也可以存没有发送出去的一个草稿，比如说我在输入框输入了一些东西，我人走了，或者是要切换到其他对话看一些东西。当前会话的输入草稿，用户ui的一些偏好。但是如果说业务需要大量的一个缓存，要考虑indexedDB

 indexDB  sessionstorage   localstorage  cookie

localstorage  怎么可能跨设备呢

前端缓存解决的是，当前这个设备临时的体验问题，一旦进行到跨设备的，肯定依赖于服务端的一个持久化，如果说用户换了设备，你还能再去看当前的一个conversation的话，那肯定是有注册的一个用户id，有对应的用户id的conversation api，把历史的message信息按照分页的方式把他拉到本地

进一步补充，localstorage搞的这个草稿箱不需要跨端同步，本地尚未提交的一种本地状态



##### 问：如果数据量很大呢

indexDB   要么就是后端存储

##### 问：所以你缓存的只是用户的输入，有大模型的输出吗。你缓存的只是用户输入框里面的内容，那用户如果输入框有上传图片和文件呢？

大模型输出肯定不是缓存到这个里面的啊

附件上传架构的问题，如图片，文件。千万不要说把图片放在localstorage里

1.前端去进行校验

（1）type，是pdf、excel、word、markdown

（2）校验size大小 ，对文件数量进行一定限制 count number

后端支不支持这么大的size，后端最多支持同时并发数是多少，公司的业务支不支持pdf、md、图片

2.看存在哪里，一般可以存在oss（专门存图片大的库），这个大的服务器的库会返回一个url给你，再去把message上传上去。message {content: " 今天天气怎么样"，attachment: url}，

文件的本身是不会放到localstorage里的，作为前端要先校验，再通过upload的api去进行上传，如果大文件的话，很有可能要去进行断点上传，分片上传



##### 问：那你刚刚不是说跨端吗，（换个设备还能看历史对话）那你们这个跨端存储不是就没有意义。还是说前端的这个存储，是因为它其实还没有发送出去，只是做一个简单的简单的存储



##### 问：那你这个前后端，因为是流式的返回嘛，你们是通过什么样的交互方式，就是接口，前端是用什么方式去拿到后端的数据呢

ai对话和普通的api最大的一个区别，response API =>不是把所有的数据一次性返回的，不是一次性返回完整的json，通常要采用sse，

如果是fetch的话，const reader = response.body.getReader()，然后我们就会得到，由前端不断地去读这个文件，const { value, done } = await reader.read()，每次都收一个chunk，追加到我们当前这个message里面，直到done的值变成completed，流式输出就完成了。Response是怎么样得到的：const response = await fetch(XXX)。

普通的restful的api和ai对话的api，返回时怎样处理的



前端是用什么方式去拿到后端的数据呢：要么用fetch，要么用axios

##### 问：就是axios调接口呗，拿到东西前端是怎么处理的呢，

http会有一个response，response就是流，流用一个reader进行处理和接收，不断地await之后呢，read变量不断地去read，不断地得到value和done，直到done的值是complete，就结束了

进一步追问，ai chat通常，对于典型的LLM，客户端发一个request，服务端持续性的往客户端推这个token，token说白了就是一段chunk，一段chunk，通常sse就够了，fetch的streaming就够了

接口这分为几个点，普通请求用restful API，服务器持续的向客户端发送数据，就SSE，或者就上面写的伪码，或者就是websocket



##### 问：那你怎么判断后端这个有没有结束，因为结束了，你肯定要给用户一个反馈，没结束是不是也有一个反馈是正在什么生成中之类的呢

就value和done，没结束，done为false，结束了为true，或者commplete啥的



##### 问：那你这个思考过程和那个正式的回答之间有什么区分吗

怎么知道ai回答结束，不是看有没有数据，是看done值，

不能把模型内部的推理简单理解成一段思维的链去进行一个展示，假如说产品想要ai感知用户当前的一个工作状态，要设计一个结构化事件。ai其实是一个大脑，没有存任何信息的，人的大脑不断训练，记忆存在了海马体里面，真正ai就是大量参数的一个过程，比如说我要去杭州，ai会去查杭州天气，其实是一个web search的一个和过程，其实不是ai的能力，web Search只是ai的手和脚，称之为触耳方式，除了查天气，还要去大众点评一些已有的里面去规划有哪些景点，就会设计整个结构化的一个事件，比如说检索知识库，比如说search，tool调用，前端根据不同的type去进行不同的一个ai的渲染，后端大概是返回这个东西给{type:"search_web", data: "xxx"}，然后第二次{type: "tool_XXX", data: "xxx"}，第三次是其他的，type不同去进行不同ai的渲染，思考过程不是展示在chat的下面吗，最终把独立思考展示在独立思考这里

tools function

##### 问：像这种回答类的，我理解数据量应该会很大吧，会有什么性能问题

后端sse输出，每次一个chunk，不可能数据量大，sse整个过程本质上就把一个很大的数据已经切分了，

ai输出有性能问题，你高频的token，就是chunk传到前端，会导致前端不断地去进行一个render，这块才是前端性能，因为你的token，如果是批量，比如说20毫秒就有一次，会进行update，其实是一个高频状态更新，频繁render，前端要做一个buffer加一个batching，统一多少毫秒刷新一次ui，

setState不是立即，是一个回调的过程，异步的过程，等我们把整个这个setstate更新好之后才会渲染当前页面，高频的token不断地从后端往前刷，是高频render问题

dom节点越来越多，可能会采取分页或者虚拟列表的问题，

一次请求只请求一部分，本质上和分页是一致的，往上刷新，相当于点击第二页这种

还有历史的message，肯定不是前端，内存就爆炸了

##### 问：那如果我历史对话很多，可能几十条几百条，那我一起一直往上滚，你这个都是存在你本地的吗

Conversation API 分页

默认加载一页大概多少条，不断往上滚，就会请求下一页，

dom数量大，就考虑虚拟列表，只渲染viewport附近的就可以



##### 问：后端返回的是纯文本还是带格式的，md。那回答里面有没有涉及到一些表格或者图片的展示，你会怎么去做

ai输出的一般使用就是markdown，前端不是直接的innerhtml，是经过markdown  parser转化成的受控组件，所以普通文本直接展示，代码块用code block去包裹，图片用image，后端返回type不同采取不同受控组件进行渲染解析



##### 问：我看你简历提到通过高级检索组件复用减少代码60%，可以具体说下是什么吗

components通用能力，比如说查询重置展开等能力怎么放到组件内部的，要了解一下props是咋传的，讲清楚props对外的一个接口，

##### 问：你们抽公共组件会考虑的一些点是什么，就是哪些是需要把他抽出开的，是通过什么样的方式就是给外部去使用你的，比如说可以传入你的属性，或者是你可以传入你的方法。讲一讲封装组件的思路吧



##### 问：父子组件通信，你们封装组件的时候有没有用过插槽之类的，



##### 问：实际的工作中，有做过哪些前端性能优化相关的

之后讲



##### 问：你刚刚说那个env的一个文件配置，是不同城市有自己不同的自定义配置吗，那里面具体是针对一些什么做配置，怎么作用到代码里

##### 问：所以每个都有自己定制的这个header组件是吗，并不是说通过一个属性就可以自动同一个组件自动自适应对吗。

let cityConfig

Header 组件 + cityConfig去进行渲染



##### 问：你刚刚说用到vite分包是吧，路由懒加载

自己看

##### 问:首屏加载速度提升了45%。还有其他首屏加载提升的一些方式吗

之后讲



##### 问：你在实际项目中遇到啥重点难点，是咋解决的

##### 问：为什么要用浏览器原生的下载去做这个，这个是需要前端去对比吗



##### 问：那你有做过文件上传吗

multipart/form-data 实现文件以及字段上传

课后作业2：前端序列化和反序列化

大文件上传，file去进行切片



##### 问：比如说现在有俩系统，A和B，他们是用不同的框架去实现的，比如vue2和vue3，那如果我想在系统A中去引用系统B的一个组件，或者你可以先说页面也行



##### 问：这不行吧，这都是独立部署的，两个系统完全独立的，那你之前有了解过微前端吗，你用过iframe这种方式吗

https://qiankun.umijs.org/   多个子应用分批进行部署，由qiankun本身去做通信

iframe，http一个标签

##### 问：你实际工作中咋用ai，做些啥，你用它辅助你，有问题再问他还是直接给你生成代码

##### 问：那你咋确定他写的东西符不符合你的要求，从哪几个方面

##### 问：你刚刚说到一个，你会去验证它影响到的地方，这其实是做一个影响分析，你咋做的，纯靠人的经验，还是会有一些别的手段，

AI 工作流问题，之后讲
